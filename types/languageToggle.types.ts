export type LanguageTogglePlacement = 'fixed' | 'inline';

export type LanguageToggleProps = {
  /** Homepage/legal use fixed; staking header uses inline. */
  placement?: LanguageTogglePlacement;
};
